import React, { useState } from "react";
import { Document, HeadingLevel, ImageRun, Packer, Paragraph, TextRun } from "docx";
import { saveAs } from "file-saver";
import { extractLocalDocumentText, renderScannedPdfPages } from "./extractLocalDocumentText";

const isPdf = (file) => file?.name?.toLowerCase().endsWith(".pdf");
const safeName = (file) => file.name.replace(/\.[^.]+$/, "").replace(/[^a-z0-9_-]+/gi, "_") || "firmaxis_document";

export default function DocumentConversionHub() {
  const [file, setFile] = useState(null); const [inputKey, setInputKey] = useState(0); const [text, setText] = useState(""); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const [ocrFailed, setOcrFailed] = useState(false);
  const clear = () => { setFile(null); setText(""); setMessage(""); setError(""); setOcrFailed(false); setInputKey((key) => key + 1); };
  const extract = async () => { if (!file) return setError("Choose a document first."); setBusy(true); setError(""); setMessage(""); try { const result = await extractLocalDocumentText(file); const noText = !result.replace(/\[VERIFY:[^\]]+\]/g, "").trim(); setText(result); setOcrFailed(noText); setMessage(noText ? "OCR could not read this scan. You can still download a visual Word copy containing the scanned PDF pages." : "Text extracted locally. Review it before downloading the editable Word file."); } catch (err) { setError(err.message || "Document conversion failed."); } finally { setBusy(false); } };
  const downloadText = async () => { if (!file || !text.trim()) return; setBusy(true); try { const word = new Document({ sections: [{ children: [new Paragraph({ text: "FirmAxis — Editable Document Copy", heading: HeadingLevel.HEADING_1 }), new Paragraph({ children: [new TextRun({ text: `Source file: ${file.name}`, italics: true })] }), new Paragraph({ children: [new TextRun({ text: "Review OCR-extracted details and layout before use.", italics: true })] }), ...text.split(/\r?\n/).map((line) => new Paragraph({ text: line || " " }))] }] }); saveAs(await Packer.toBlob(word), `${safeName(file)}_editable.docx`); setMessage("Editable Word document downloaded."); } catch (err) { setError(err.message || "Could not create the Word document."); } finally { setBusy(false); } };
  const downloadVisual = async () => { if (!file || !isPdf(file)) return; setBusy(true); setError(""); try { const pages = await renderScannedPdfPages(file, 50); const children = [new Paragraph({ text: "FirmAxis — Visual Copy of Scanned PDF", heading: HeadingLevel.HEADING_1 }), new Paragraph({ children: [new TextRun({ text: `Source file: ${file.name}. Pages are images, not editable text.`, italics: true })] })]; pages.forEach((page, index) => { children.push(new Paragraph({ text: `Page ${index + 1}` })); children.push(new Paragraph({ children: [new ImageRun({ data: page.data, transformation: { width: Math.min(500, page.width), height: Math.min(700, page.height) } })] })); }); const word = new Document({ sections: [{ children }] }); saveAs(await Packer.toBlob(word), `${safeName(file)}_visual_copy.docx`); setMessage("Visual Word copy downloaded with scanned PDF pages."); } catch (err) { setError(err.message || "Could not create the visual Word copy."); } finally { setBusy(false); } };
  return <div className="min-h-screen bg-slate-950 p-6 text-slate-100"><div className="mb-6 rounded-xl border border-slate-800 bg-slate-900 p-5"><p className="text-[10px] font-bold uppercase tracking-widest text-indigo-400">Local document utility</p><h1 className="mt-2 text-2xl font-black">Document Conversion Hub</h1><p className="mt-1 text-xs text-slate-400">Convert MCA, tax, litigation, and client documents without uploading the source file to Supabase.</p></div><div className="grid grid-cols-1 gap-6 xl:grid-cols-5"><section className="rounded-xl border border-slate-800 bg-slate-900 p-5 xl:col-span-2"><h2 className="text-xs font-bold uppercase tracking-wider text-slate-400">Select source document</h2><input key={inputKey} type="file" accept=".pdf,.png,.jpg,.jpeg,.docx,.xlsx,.xls,.txt,.csv" onChange={(event) => { setFile(event.target.files?.[0] || null); setText(""); setMessage(""); setError(""); setOcrFailed(false); }} className="mt-4 text-xs" />{file && <div className="mt-3 flex items-center justify-between gap-3 rounded-lg border border-slate-700 bg-slate-950 p-3 text-xs"><span>{file.name} · {Math.ceil(file.size / 1024)} KB</span><button onClick={clear} className="rounded border border-slate-600 px-2 py-1 text-[10px] font-bold">Remove</button></div>}<div className="mt-4 rounded-lg border border-indigo-900 bg-indigo-950/40 p-3 text-[11px] leading-relaxed text-indigo-100"><b>Supported:</b> PDF, scanned PDF, PNG, JPG, DOCX, XLS/XLSX, TXT, CSV.<br /><b>Scan limit:</b> only the first 50 scanned PDF pages are processed using browser OCR.<br /><b>Fallback:</b> if OCR fails, a visual Word copy can preserve scanned PDF pages as images.<br /><b>Privacy:</b> the source file is not uploaded or stored.</div>{error && <p className="mt-3 rounded bg-rose-950 p-3 text-xs text-rose-200">{error}</p>}{message && <p className="mt-3 rounded bg-emerald-950 p-3 text-xs text-emerald-200">{message}</p>}<button disabled={!file || busy} onClick={extract} className="mt-4 w-full rounded bg-indigo-600 py-2.5 text-xs font-bold uppercase disabled:opacity-50">{busy ? "Processing locally…" : "Extract editable text"}</button>{ocrFailed && isPdf(file) && <button disabled={busy} onClick={downloadVisual} className="mt-2 w-full rounded border border-amber-500 bg-amber-950/50 py-2.5 text-xs font-bold uppercase text-amber-100 disabled:opacity-50">Download scanned pages as Word</button>}</section><section className="rounded-xl border border-slate-800 bg-slate-900 p-5 xl:col-span-3"><div className="flex items-center justify-between gap-3"><div><h2 className="text-xs font-bold uppercase tracking-wider text-slate-400">Editable text preview</h2><p className="mt-1 text-[10px] text-slate-500">Correct OCR errors before downloading.</p></div><button disabled={!text.trim() || busy} onClick={downloadText} className="rounded border border-slate-600 bg-slate-800 px-3 py-2 text-xs font-bold disabled:opacity-50">Download Word (.docx)</button></div><textarea value={text} onChange={(event) => setText(event.target.value)} placeholder="Choose a document and select Extract editable text…" className="mt-4 h-[480px] w-full rounded border border-slate-700 bg-slate-950 p-3 font-mono text-xs leading-relaxed text-slate-200" /></section></div></div>;
}
